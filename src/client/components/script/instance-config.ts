import { Button, TextField } from "mdui";

const initlistener = () => {
  const textfield = document.getElementById(
    "instance-config-text-field"
  )! as TextField;
  const button = document.getElementById("instance-config-button")! as Button;
};
export default initlistener;
